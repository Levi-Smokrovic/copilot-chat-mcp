# Optional — copy to .env (or set in your MCP client's env block) to enable Telegram.
# Create a bot with @BotFather to get the token.
TELEGRAM_BOT_TOKEN=
# Leave blank to auto-lock to the first chat that messages the bot.
TELEGRAM_CHAT_ID=

# Web UI
HOST=127.0.0.1
PORT=8765
